import { ListItem } from "react-native-elements";

<ListItem
  leftAvatar={{
    title: name[0],
    source: { uri: avatar_url },
    showEditButton: true
  }}
  title={name}
  subtitle={role}
  chevron
/>;
